index=* sourcetype=*access* url="*/health*"
| stats 
    count as total_checks
    count(eval(status=200)) as healthy
    count(eval(status!=200)) as unhealthy
    by status url
| eval health_percentage=round((healthy/total_checks)*100,2)
| sort -total_checks
